rikaikun with sidebar
=====================

A Modified rikaikun with sidebar for looking up Japanese words.

To use this, first download,
then open Google Chrome extensions page, check "Developer mode" and click Load an unpacked extension -> point to downloaded unziped folder.

